const mserver = require("../mserver.js");

let execute = async function(ARGUMENTS, message) {

};

module.exports = {
    execute,
    info: {
        name: "playerinfo",
        descrip: "Get information about a player on the mindustry server",
        longDescrip: "Work in progress" // TODO
    }
};